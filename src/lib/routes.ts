/**
 * Route paths, kept out of `routes.tsx` so screens can link to them without
 * importing the router module that lazy-loads those same screens.
 */
export const basicRoutes = {
  start: '/',
  home: '/home',
};
